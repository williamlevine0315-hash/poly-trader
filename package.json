{
  "name": "poly-trader",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild worker.js --bundle --format=esm --platform=browser --main-fields=browser,module,main --conditions=browser --define:global=\"globalThis\" --outfile=dist/worker.mjs"
  },
  "dependencies": {
    "@polymarket/clob-client": "2.2.0",
    "ethers": "5.7.2"
  },
  "devDependencies": {
    "esbuild": "0.21.5"
  }
}
